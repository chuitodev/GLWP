services:
  invitation:
    build: .
    container_name: glwp-invitation-local
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./storage/uploads:/app/storage/uploads
      - ./storage/data:/app/storage/data
